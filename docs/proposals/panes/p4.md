# P4 — ADVERSARIAL REVIEW · harness-ready proposals

**Date:** 2026-07-29 · **Pane:** p4 · **Workspace:** ws83621x0 · **Coordinator:** p0  
**Mode:** review report only — no application code · no proposal rewrites  
**Weapon:** BASE44-PROMPTS.md R1–R10 + P4 checklist  
**Ground truth:** `docs/proposals/P0-ALIGNMENT.md` (incl. **§6 ERRATA**), `ui/HANDOFF.md`, `ui/src/lib/agentBridge.js` / `tauriAgentBridge.js`  
**Inputs reviewed:** `panes/p1.md`, `panes/p2.md`, `panes/p3.md`, `panes/p5.md`

**Method:** hostile item-by-item pass. Every finding cites exact proposal text. Severity: **BLOCKING** (must fix before operator picks an id for P5) / **SHOULD-FIX** / **NIT**. Non-violating items are not praised inline — survivors appear only in Top 5.

**ERRATA applied (P0 §6):** Templates are already local-first (`base44Client.js` → `localAgentTemplateStore.js`); PRIORITY + AUTONOMY selects have no backend effect (M1); remaining P0 honesty findings stand.

---

## 1 · Item inventory (reviewed)

| Source | Ids | Count |
|---|---|---|
| p1 | F-OBS-1…4, F-TPL-1…2, F-MEM-1…2, F-ORCH-1…2, F-ONB-1…2, F-PAL-1, F-SKL-1, F-GATE-UI-1, F-MCP-UI-1 | 16 |
| p2 | U-CMD-1…3, U-ORCH-1…2, U-PAL-1, U-ONB-1, U-QUE-1…2 | 9 |
| p3 | A-VIS-1…10 | 10 |
| **Total proposal items** | | **35** |

Cross-cutting / competitive / sequencing issues are extra findings (not double-counted as items unless tied to an id).

---

## 2 · Violations (by severity)

### 2.1 BLOCKING

---

#### V-B1 · U-ORCH-1 · R3 (fake affordance) + P0 §6 M1

**Violation:** R3 — controls without backend effect presented as live spawn knobs.

**Evidence (p2 U-ORCH-1 ux):**
> `Overlay/sheet fields: harness, model (if applicable), priority, autonomy ceiling (existing), **Role** segmented…`

**Ground truth (P0 §6 M1):**
> `PRIORITY + AUTONOMY selects in NewAgentOverlay.jsx:61-73 and TemplateBuilder.jsx:31-40 have **no backend effect** — Tauri spawn_workspace takes only id, harness, repo, role`

Verified: `tauriAgentBridge.js` `spawn_workspace` invoke passes `{ id, harness, repo, role }` only — no `priority` / `autonomy`.

**Fix:** Drop interactive PRIORITY/AUTONOMY **or** tag both **NEEDS-BACKEND** + disable until contract accepts them. Do not leave “existing” as implied-honest. Same for TemplateBuilder when U-ONB-1 / F-TPL reuses that form.

**Severity:** BLOCKING

---

#### V-B2 · U-CMD-1 · R3 (registry-reconciling / stale badges without NEEDS-BACKEND)

**Violation:** R3 — ships UI states that require a single liveness authority not on the contract, without tagging NEEDS-BACKEND on those states.

**Evidence (p2 U-CMD-1 STATES):**
> `| **stale** | Single source badge **"registry reconciling / stale"** on affected panes (DB §6) — not "live" in one place and "gone" in another. |`  
> `| **registry-reconciling** | Fleet-wide or per-pane badge from one authority; mutations may still work; UI does not claim full registry parity until resolved. |`

**backend_dep for U-CMD-1 lists only EXISTS methods** (`subscribe`, `spawnAgents`, …) — no NEEDS-BACKEND for Phase 0 liveness source. P0 §2 / ranked #2: registry reconciling is **NEEDS-BACKEND** (same as p1 F-GATE-UI-1, which correctly gates).

**Fix:** Split honesty fixes (empty count, SessionInfo, LOAD DEMO) from liveness badges. Tag reconciling/stale strip as **NEEDS-BACKEND · do not ship until Phase 0** (mirror F-GATE-UI-1). Until then, omit or show static “pending design” only.

**Severity:** BLOCKING

---

#### V-B3 · F-TPL-1 · R9/R3 honesty + P0 §6 E1 (false premise / wrong scope)

**Violation:** Proposal frames work as removing a cloud tether that **does not exist**. Risks operator picking “swap Base44 entity” P5 work that is already done, or re-introducing confusion as if R6 were still broken.

**Evidence (p1 F-TPL-1):**
> `title: Local AgentTemplate store (JSON/SQLite) with storage badge`  
> `icp_fit: Local-first / no cloud tether for team spawn recipes — removes last Base44-entity dependency`  
> `ux: … add footer badge Local (not hosted).`  
> `backend_dep: … NEEDS-BACKEND: none for pure UI-local store … New module e.g. ui/src/lib/templateStore.js`

**Ground truth (P0 §6 E1):**
> `ui/src/api/base44Client.js is an **offline local stand-in** … Templates are **already local-first**`  
> `§4 ranked item #3 re-scopes: R-TEMPLATES remaining work = JSON import/export + schema pin + recipes coupling … **not** "remove cloud tether".`

**Fix:** **Rewrite scope** (not drop the id family): F-TPL-1 → optional rename/badge/docs honesty (“Local” / drop base44 silhouette confusion) **or** merge residual into F-TPL-2 (import/export + schema pin). Do not propose a greenfield `templateStore.js` as if no store exists.

**Severity:** BLOCKING (wrong P5 target if picked as written)

---

#### V-B4 · U-ONB-1 · R3 + E1 (Base44 swap + autonomy as effective)

**Violation:** Same false Base44 tether + recipes that “set gates” while autonomy fields are inert (M1).

**Evidence (p2 U-ONB-1):**
> `Storage badge **"Local"** (not Base44). Import/export JSON. Apply recipe → sets form defaults / spawns with profile — does not auto-grant permissions silently; shows what gates will be set.`  
> `backend_dep: **NEEDS-BACKEND** for dedicated recipe store if separate from templates; **EXISTS path:** swap templates from Base44 → local (R-TEMPLATES) then extend schema. … Autonomy fields must map to real spawn config keys only.`

**Conflict:** “map to real spawn config keys only” is correct advice, but **existing keys `priority`/`autonomy` are not real** (M1). “swap templates from Base44 → local” is obsolete (E1).

**Fix:** Reframe store as already local; recipes document **recommended** operator settings only until gate mutation EXISTS; never apply inert select values as if spawn honored them. Couple recipes to F-TPL-2 DoD, not “Base44 removal.”

**Severity:** BLOCKING

---

#### V-B5 · Cross-surface MONITORING hero · R4 + cross-consistency (F-OBS-1 vs U-CMD-2 vs A-VIS-2/3)

**Violation:** Same surface designed three incompatible ways; shipping the chart restyles as “monitoring done” would re-entrench vanity chrome R4 forbids as primary.

**Evidence:**

p1 F-OBS-1:
> `Monitoring.jsx becomes the primary surface: a dense table/strip, one row per live pane … Explicitly **no** vanity aggregate-only dashboard as the hero; aggregates (if any) are secondary`

p2 U-CMD-2:
> `Open on fleet resource area + per-agent bars bound to real signals when R-OBS lands.`

p3 A-VIS-2 / A-VIS-3 / §7:
> restyle `ResourceChart`, `SuccessRateChart`, `StatCard` as the Monitoring component spine (before→after of **chart/KPI** surfaces, not a health table)

p5 §1 recommendation:
> `prioritize F-OBS-1 health table + honesty states over vanity charts; KPI cards secondary; series chart only on real sample history`

**Fix:** Coordinator must lock IA: **table-first (F-OBS-1) + honesty chrome**; charts/KPI only secondary and only with real samples / PLACEHOLDER chip. Demote or sequence A-VIS-2/3 **after** F-OBS-1 structural replace (or re-scope A-VIS to tokens + placeholder chip on whatever remains). Do not P5 a SuccessRateChart restyle while AGENT-001 phantoms remain.

**Severity:** BLOCKING (operator id-pick ambiguity + R4 risk)

---

### 2.2 SHOULD-FIX

---

#### V-S1 · F-ONB-1 · R3 (autonomy profiles vs M1)

**Evidence (p1 F-ONB-1):**
> `each recipe = harness kind + autonomy profile (which gates/ceilings) + first-run step list`  
> `Apply recipe → prefill spawn form + document gate checklist`  
> `Autonomy gate **mutation** of running MCP server is out of band — recipe documents recommended settings`

Prefill of spawn form **includes autonomy fields that do nothing** (M1) unless explicitly stripped. Partial self-protection exists (“do not show toggles for gates that cannot be set”) but does not name PRIORITY/AUTONOMY as already-shipped fake affordances.

**Fix:** Explicit kill/disable list for inert fields; recipes may only prefill harness/role/repo (and other EXISTS fields).

**Severity:** SHOULD-FIX

---

#### V-S2 · p5 scout baseline + R-TEMPLATES rec · superseded by E1 (competitive / synthesis)

**Evidence (p5 baseline):**
> `templates still Base44-tethered`  
> `Remaining on Base44 **ERODE**s trust`  
> `Ship as proposed … removes last cloud tether`

**Fix:** Coordinator synthesis must apply E1: templates rec = import/export + schema pin + recipes coupling only. Scout’s competitive verdict (MATCH hygiene) still holds for portability; **cloud-tether narrative is false**.

**Severity:** SHOULD-FIX (synthesis correctness)

---

#### V-S3 · F-OBS-1 / F-OBS-2 · R4 partial (columns that cannot fill without backend still listed as primary UX)

**Evidence (p1 F-OBS-1 ux):**
> `columns bound to the real-signal list — process liveness · per-child CPU · per-child mem · git state (branch/dirty) · last-tool-failure · human-gate queue depth`

backend_dep correctly tags CPU/mem/git/failure as NEEDS-BACKEND, and fake_affordance says cells render `no data` — **good**. Risk remains that a naive P5 builds the full column set with empty/`0` cells that *look* like a finished differentiator.

**Fix:** Ship columns only for EXISTS signals first (id, kind, status, attention, branch/worktree from subscribe); add metric columns behind NEEDS-BACKEND / `no data` with source chip — never zeros. Prefer phased DoD matching BUILD-PLAN 1a then 1b.

**Severity:** SHOULD-FIX

---

#### V-S4 · U-ORCH-2 · R3/R10 (orchestrate bridge methods as product path)

**Evidence (p2 U-ORCH-2):**
> `backend_dep: **NEEDS-BACKEND** for team_orchestrate-style preview in bridge if UI only has spawn today. NOTE: bridge.orchestratePreview(plan) → { roster, collisions[] } / bridge.orchestrateDispatch(plan). Exists conceptually in agent-teams MCP; UI bridge coverage must be explicit before ship.`

Tagging is mostly correct. Risk: “Exists conceptually in agent-teams MCP” can be misread as **agentBridge.js EXISTS**. Palette dual-path also lists Orchestrate as a first-class row (U-PAL-1).

**Fix:** Keep NEEDS-BACKEND until `agentBridge` methods exist; local client-side collision + multi-`spawnAgents` only for v1; no palette “Orchestrate” row until handler opens a real flow (U-PAL-1 already states this — enforce in synthesis).

**Severity:** SHOULD-FIX

---

#### V-S5 · A-VIS-2 + §7.3 SuccessRateChart · R4 (vanity series restyle pressure)

**Evidence (p3 §7.3 / A-VIS-2):**
> restyle “TASK SUCCESS RATE — PER AGENT” with banded lane fills; placeholder chip required until R-OBS  
> p1 F-OBS-2: `Drop or demote "Tasks Completed" until real outcome events exist`  
> P0 §2: task-outcome events **NOT on contract**

**Fix:** A-VIS-2 may bind **CPU/mem chart chrome + PLACEHOLDER** only; defer SuccessRateChart / Tasks Completed visual investment until outcome events EXISTS. Align with p1 demotion + p5 “Do not ship interpolated success rates.”

**Severity:** SHOULD-FIX

---

#### V-S6 · Cost/token telemetry gap (p5) · competitive sanity — not in p1–p3

**Evidence (p5 §1 / §10 / §12):**
> `Reshape optional follow-on: add a thin **cost/token strip when harness exposes it**`  
> `Known market gap outside current proposal spine: Vibeyard-grade cost/token/context meters — fold into R-OBS later`

No F-OBS / U-CMD / A-VIS item absorbs this reshape. Not an R2 SKIP — it is a flagged **table-stakes-missing** axis.

**Fix:** Optional NOTE under F-OBS-2: cost/token strip when harness exposes samples; NEEDS-BACKEND; never delay process-liveness honesty. Not a new pillar.

**Severity:** SHOULD-FIX (product completeness vs scout — not a rule fail on existing items)

---

#### V-S7 · F-ORCH-1 alone vs p5 demote-if-cosmetic · competitive + R1 depth

**Evidence (p1 F-ORCH-1):**
> `replace free-text-only role with segmented control of four closed roles`  
> `fake_affordance_check: PASS — roles are advisory conventions`

p5 §4:
> `If implementation is only cosmetic role pills, **demote** — it will not beat Intent or Claude Teams.`

Role cast without owned-paths/preview (F-ORCH-2 / U-ORCH-2) is table-stakes labeling. Not an R-rule fail, but **weak P5 candidate if isolated**.

**Fix:** Bundle F-ORCH-1 with F-ORCH-2 (or U-ORCH-*) for any ship decision; do not pick role pills alone.

**Severity:** SHOULD-FIX (selection policy)

---

#### V-S8 · Missed P0 §6 micro-honesty (M2–M8) across p1–p3

**Evidence:** p1 F-OBS-4 / A-VIS-7 cover LOAD DEMO, SessionInfo, live count, Monitoring “Live”. **None** list:

- M2 mock sharing methods missing  
- M3 empty workspace cap-blame copy  
- M4/M5 wrong shortcut tooltips (BROADCAST / ⌘G layout)  
- M6 unrouted auth pages  
- M7 WorkspaceTile ignore rename/delete  
- M8 mock sendRaw no-op surprise  

**Fix:** Extend F-OBS-4 (or a sibling micro-honesty id) with M1–M8 checklist so P5 honesty PR does not leave half the P0 audit.

**Severity:** SHOULD-FIX

---

#### V-S9 · U-CMD-1 keyboard map · R7 partial (proposes chords; dual-path OK, but overclaims “exists”)

**Evidence (p2 Global KEYBOARD map):**
> `Alt+↑/↓ | Workspace cycle | YES (per DB)`  

P0 §3 keyboard map only lists ⌘⇧I, ⌘G, Tauri zoom menu — **not** Alt+↑/↓ as verified EXISTS. If DB claims it but code lacks it, this is a fake dual-path.

**Fix:** Verify against `useKeyboardShortcuts.js` before claiming YES; else PROPOSED + add handler with palette row same PR.

**Severity:** SHOULD-FIX

---

#### V-S10 · A-VIS §1.2 / chart-3 · R5 mild (network series on `--success`)

**Evidence (p3 §1.2 chart tokens):**
> `| --chart-3 | --success | Network / success series (when not danger) |`

Network throughput is non-state; mapping to sacred success lane fights §1.4 (“Prefer secondary stroke … not a 4th sacred lane”) and R5 “state colors never wear brand / non-state.”

**Fix:** Prefer `--chart-secondary` / muted info for network; reserve `--success` for success/healthy outcomes only.

**Severity:** SHOULD-FIX

---

### 2.3 NIT

---

#### V-N1 · F-MEM-1 · R5 mild

**Evidence:** `selected node --need outline` — uses attention lane for selection chrome, not needs-human state.

**Fix:** Use `--info` or neutral selected ring; reserve `--need` for attention/warn.

**Severity:** NIT

---

#### V-N2 · U-CMD-1 · R10 framing

**Evidence:** backend_dep lists `list_queue poll path` among EXISTS bridge methods. `list_queue` is Tauri internal to `subscribe` poll, not a public `agentBridge` method operators call.

**Fix:** Say EXISTS: `subscribe` (fed by `list_queue` under Tauri).

**Severity:** NIT

---

#### V-N3 · HANDOFF stale vs E1 (doc debt, not a proposal id)

**Evidence:** `ui/HANDOFF.md` still says templates “persisted via the Base44 AgentTemplate entity — swap for a local store”. Proposals mirrored HANDOFF; E1 corrected reality.

**Fix:** HANDOFF errata outside P4; proposals must follow P0 §6 not stale HANDOFF.

**Severity:** NIT (for P4 report; SHOULD-FIX for repo docs later)

---

#### V-N4 · U-QUE-1 ranking vs HANDOFF scores

**Evidence:** HANDOFF suggests error=400 > needs_input=300; U-QUE-1 prioritizes needs_human first and invents section A/B/C. Explicit product choice, not R-fail; can confuse implementers.

**Fix:** One ranked key in synthesis; note intentional divergence from HANDOFF numeric sketch.

**Severity:** NIT

---

#### V-N5 · A-VIS-9 / data-skin · R2 proximity (clean)

**Evidence:** A-VIS-9 correctly ships **no switcher** (SKIP theme gallery). No violation — noted so synthesis does not “complete” skins UI.

**Severity:** NIT (watch only)

---

#### V-N6 · Duplicate design of CONTEXT / recipes / palette (ids differ — OK)

F-MEM-1 ≈ U-CMD-3; F-ONB-1 ≈ U-ONB-1; F-PAL-1 ≈ U-PAL-1; F-ORCH-* ≈ U-ORCH-*. Different prefixes, overlapping intent — expected P1/P2 split. Not id collision.

**Fix:** Coordinator merge table: one canonical id per capability for P5 pick list.

**Severity:** NIT

---

## 3 · Rule-by-rule scoreboard (aggregate)

| Rule | Worst finding | Notes |
|---|---|---|
| **R1 ICP** | Clean on SKIP beginners; F-ORCH-1 alone is shallow for power-user differentiation (V-S7) | No beginner-only item smuggled as core |
| **R2 SKIP** | **PASS** | No drag-kanban, voice, video hub, vendor bench, theme gallery UI, three modes, cloud-first. Recipes ≠ courses. A-VIS-9 no switcher |
| **R3 fake affordance** | **FAIL** V-B1, V-B2, V-B4; V-S1 | PRIORITY/AUTONOMY; registry badges; recipe prefill |
| **R4 monitoring honesty** | **AT RISK** V-B5, V-S3, V-S5 | Table-first vs chart restyle; success-rate chrome |
| **R5 lanes / tokens** | V-S10, V-N1 | Mostly solid in p3; network→success mapping |
| **R6 local-first** | **PASS** on new surfaces; **narrative fail** V-B3/V-S2 (false cloud tether) | No credits/upsell proposed |
| **R7 keyboard** | V-S9 | Dual-path discipline good in U-PAL-1; verify Alt+↑/↓ |
| **R8 a11y** | **PASS** (spec level) | Focus, color+label, reduced-motion in p2/p3 |
| **R9 honesty** | V-B3 false Base44 claim | No invented BridgeMind REST/webhooks/CLI/fleet KPI |
| **R10 boundary** | V-N2; notes mostly OK | Method signatures as NOTES; orchestrate methods tagged NEEDS-BACKEND |
| **Sequencing** | **PASS** critical path | Phase 0 → R-OBS; templates/mem/orch next; palette/skills opportunistic — matches BUILD-PLAN §1 / p5 §9 |
| **Competitive (p5)** | V-S2, V-S6, V-S7 | ERODE items not re-smuggled; cost strip gap unaddressed; cosmetic roles demote |

---

## 4 · Clean / near-clean items (no BLOCKING of their own)

These survived the attack without BLOCKING defects **as written** (may still inherit cross-cuts):

| Id | Note |
|---|---|
| **F-OBS-4** | EXISTS-only honesty; Demo/SessionInfo/Live copy — strongest micro PR |
| **F-OBS-1** | States + NEEDS-BACKEND metrics notes correct; subject to V-S3 column phasing + V-B5 IA lock |
| **F-OBS-2** | Source/age + no interpolation — solid if secondary to table |
| **F-OBS-3** | Explicitly secondary; network omitted until real — good R4 |
| **F-TPL-2** | Import/export aligns E1 residual DoD |
| **F-MEM-1 / U-CMD-3** | Read-only + NEEDS-BACKEND graph; no write HD overclaim |
| **F-MEM-2** | Focus only on match — clean |
| **F-ORCH-2** | Advisory warn, no enforce toggle — design-correct |
| **F-PAL-1 / U-PAL-1** | Handler-first rule; typing-target — clean if enforced |
| **F-SKL-1** | Thin index, no marketplace/credits — clean P2 |
| **F-GATE-UI-1 / F-MCP-UI-1** | Correctly non-shipping NEEDS-BACKEND surfaces |
| **U-CMD-2** | Placeholder until R-OBS — honesty path OK (IA vs F-OBS-1 still V-B5) |
| **U-QUE-1 / U-QUE-2** | EXISTS subscribe/sendInput/resume; RESUME-always; amber = needs-you |
| **A-VIS-1, A-VIS-4, A-VIS-5, A-VIS-6, A-VIS-8, A-VIS-10** | CSS/token/motion/focus/density — no contract lies |
| **A-VIS-7** | Aligns F-OBS-4 Demo/count honesty |
| **A-VIS-9** | Structure only, no gallery |

**Contaminated if picked alone:** F-TPL-1 (V-B3), U-ORCH-1 (V-B1), U-ONB-1 (V-B4), U-CMD-1 (V-B2), A-VIS-2/3 without IA lock (V-B5).

---

## 5 · Top 5 strongest P5 candidates

What survived cleanly enough for the operator’s shortlist (after applying BLOCKING fixes where noted):

| Rank | Id | One-line justification |
|---|---|---|
| 1 | **F-OBS-4** | Pure R3/R4 honesty on EXISTS paths (kill/wire Demo, SessionInfo, live count, “Live” copy); S effort; unblocks trust before any metrics backend; rides any Monitoring PR. |
| 2 | **F-OBS-1** | Critical-path differentiator; real-signal table + empty/loading/stale/state_blind; NEEDS-BACKEND metrics correctly noted; p5 “double down”; **after** V-B5 IA lock + V-S3 column phase. |
| 3 | **A-VIS-1** (+ **A-VIS-6** if one PR) | Sacred lane tokens + dark-default `:root` + focus rings; zero backend; prerequisite for honest chart/status chrome; p5 “tokens only” reshape. |
| 4 | **F-ORCH-2** (bundle **F-ORCH-1**) | Advisory owned-paths + amber collision WARNING (no enforce toggle) — distinctive ADAPTER; p5 BUILD only if semantic depth, not cosmetic pills alone. |
| 5 | **F-TPL-2** (re-scoped R-TEMPLATES) | Import/export + schema pin matches E1 residual DoD; local-first already true; hygiene without fake “cloud tether removal.” |

**Honorable mentions:** U-QUE-2 (RESUME-always + inline reply polish), F-MEM-1 (read-only graph when read API ready), F-PAL-1 (P2 only — do not delay critical path).

**Do not pick without rewrite:** F-TPL-1 as written, U-ORCH-1 as written, U-ONB-1 as written, SuccessRateChart-only visual P5, F-GATE-UI-1 / F-MCP-UI-1 (backend-gated).

---

## 6 · Verdict footer

| Metric | Value |
|---|---|
| **Total proposal items reviewed** | **35** (16 p1 + 9 p2 + 10 p3) |
| **BLOCKING findings** | **5** (V-B1…V-B5) |
| **SHOULD-FIX findings** | **10** (V-S1…V-S10) |
| **NIT findings** | **6** (V-N1…V-N6) |
| **Drops recommended** | **0 full id drops** · **2 hard re-scopes** (F-TPL-1 → badge/docs or fold into F-TPL-2; SuccessRateChart investment deferred) · **1 demote-if-alone** (F-ORCH-1 without F-ORCH-2) · **2 remain non-ship** (F-GATE-UI-1, F-MCP-UI-1) |
| **SKIP-list regressions found** | **0** |
| **p5 ERODE items that survived unmodified** | **0** (no kanban/voice/video/themes resurrected). **Unaddressed scout reshape:** cost/token strip under R-OBS (V-S6). **Stale scout premise:** Base44 templates (E1 / V-S2). |
| **R-rule hardest fails** | **R3** (PRIORITY/AUTONOMY, registry badges, recipe gates) · **R4 IA split** (table vs charts) · **E1 narrative** (templates) |

**Operator gate:** resolve V-B1–V-B5 in synthesis before any P5 id paste. Prefer Top-5 order above.

**Self-review (this pane):** adversarial citations only; no application code; no proposal rewrites in source panes; ERRATA §6 applied; contract claims spot-checked against `tauriAgentBridge.js` / `agentBridge.js` / P0 §2.

---

## BOUNDARIES

- **Did:** full adversarial review of every p1/p2/p3 proposal item + p5 competitive sanity; R1–R10 + cross-consistency + BUILD-PLAN sequencing; cited exact proposal text; severities BLOCKING/SHOULD-FIX/NIT; Top 5 P5 candidates; verdict footer.
- **Did not:** rewrite p1/p2/p3/p5; write or modify application code under `ui/` or `core/`; implement P5; invent BridgeMind APIs; relax R1–R10; pick the operator’s final id (recommendation only).
- **Output path:** `/Users/jeffrymilan/Personal/harness-ready/docs/proposals/panes/p4.md` (coordinator checkout only).
- **Ground-truth corrections honored:** P0-ALIGNMENT §6 ERRATA (templates local; M1 priority/autonomy inert; micro-honesty list).
- **Next owner:** coordinator synthesis → operator picks one id for P5.
